language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"
# command to install dependencies
install:
  - pip install -r numpy_unit/requirements.txt
  - pip install .
# command to run tests
script: python numpy_unit/tests.py